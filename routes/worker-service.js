/*var express = require('express');
var testo = require('../public/nnworker/queue/test.txt')

var router = express.Router();


router.get('/worker', function(req, res, next) {

  res.send(testo.test.txt('ugi in js '));
 // res.render('index', { title: 'Expressoooooooo' });
});

module.exports = router;
*/
